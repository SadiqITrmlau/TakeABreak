document.getElementById("extendButton").addEventListener("click", function() {
  swal({
    title: "Take a Break",
    text: "Do you want to extend your break time?",
    icon: "info",
    buttons: {
      cancel: "No",
      confirm: "Yes"
    }
  }).then((willextend) => {
    if (willExtend) {
      // Perform actions to extend the break time
      // Update the timer or any relevant variables
      // Call necessary functions or methods to resume the break
      swal("Break Extended!", "The break time has been extended.", "success");
    }
  });
});
